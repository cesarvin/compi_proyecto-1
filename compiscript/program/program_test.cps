class User {
  let name: string;
  let age: integer;
}

// Creamos una instancia de la clase
let user1: User = new User();

// Asignamos valores a sus propiedades
user1.name = "Alice";
user1.age = 30;

print(user1.name);